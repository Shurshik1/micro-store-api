
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Order {
  id        Int      @id @default(autoincrement())
  productName     String   
  price      Int
  buyerId Int 
  createdAt DateTime @default(now())
}